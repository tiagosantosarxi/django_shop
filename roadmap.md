### Add store configs model
### Add currency config with default € (migrations?)
### Create tax model
### Feature product multi categories
### Add decimal precision as config with default 2 (migrations?)
